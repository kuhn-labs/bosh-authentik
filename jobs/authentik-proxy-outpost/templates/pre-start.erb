#!/bin/bash
set -eu

# Create necessary directories
mkdir -p /var/vcap/data/authentik-proxy-outpost

# Set proper permissions
chown -R vcap:vcap /var/vcap/data/authentik-proxy-outpost

echo "Pre-start completed successfully"
